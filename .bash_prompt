# Set the PROMPT_COMMAND to update the branch information dynamically
# Check if the current directory is inside a git repository. If so, get the current branch name; otherwise, leave it empty.
PROMPT_COMMAND='
PS1_BRANCH=$(git rev-parse --is-inside-work-tree >/dev/null 2>&1 && git branch --show-current 2>/dev/null || echo "")
'

# Define the custom PS1 prompt (how the shell prompt looks)
PS1=''

# Add the user name in red
PS1+='\[\e[1;91m\]\u\[\e[0m\] '

# Add the static text "at" in white
PS1+='\[\e[1;37m\]at\[\e[0m\] '

# Add the hostname in blue
PS1+='\[\e[1;94m\]\H\[\e[0m\] '

# Add the static text "in" in white
PS1+='\[\e[1;37m\]in\[\e[0m\] '

# Add the current working directory in blue
PS1+='\[\e[1;34m\]\W\[\e[0m\]'

# If a Git branch is present, display "on <branch_name>" in yellow
PS1+="${PS1_BRANCH:+ \[\e[1;37m\]on\[\e[0m\] \[\e[1;33m\]${PS1_BRANCH}\[\e[0m\]}"

# Add the standard shell prompt symbol "$" in white, followed by a newline
PS1+='\n\[\e[1;37m\]\\$\[\e[0m\] '
